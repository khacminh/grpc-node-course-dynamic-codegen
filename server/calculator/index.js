const sum = require('./sum');
const decomposePrimeNumber = require('./primeNumberDecomposition');
const computeAverage = require('./computeAverage');
const getMax = require('./getMax');

module.exports = {
  sum,
  decomposePrimeNumber,
  computeAverage,
  getMax,
};
